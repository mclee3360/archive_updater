apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'archiveUpdater.controller.MainApplication'

jar {
    manifest {
        attributes ('Main-Class': 'archiveUpdater.controller.MainApplication')
    }
}
